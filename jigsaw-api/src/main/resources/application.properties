spring.application.name=jigsaw-api

# Default backend port referenced by older Play pages (e.g. localhost:8080).
server.port=8080

# /resolve result cache (Caffeine)
jigsaw.cache.resolve.maximum-size=500
jigsaw.cache.resolve.expire-after-access-hours=24
